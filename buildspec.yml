## buildspec.yml
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - npm ci
      - npm install

  build:
    commands:
      - npm run start:ci &
      - npx cypress run --record
